import React, {useState} from "react"

const Form = () => {

    const [productDetails, setProductDetails] = useState([])

    const handleSubmit = (e) => {
        e.preventDefault;
        
    }

    return(
        <form onSubmit={}>

        </form>
    )
}